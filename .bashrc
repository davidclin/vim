# Useful aliases (not VIM related)

alias ff="sudo find / -type f -name"
alias diskspace="du -cksh .[!.]* * |sort -h"

# Useful tools
$ sudo apt-get install ncdu
$ ncdu  (lists all files and sorts based on size)
